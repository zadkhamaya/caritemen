// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url  	    = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id String @id @default(uuid())
  firstName String
  lastName String
  email String @unique
  username String @unique
  password String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  events Event[]
  participate participants[]
}

model Event {
  id String @id @default(uuid())
  title String
  slug String @unique
  date String
  location String
  description String
  featuredImage String
  images String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  user User @relation(fields: [userId], references: [id])
  // categoryId String
  // category eventCategory @relation(fields: [categoryId], references: [id])
}

// still not sure on this part, lets hope it will works haha :D
// model eventCategory {
//   id String @id @default(uuid())
//   name String
//   description String
//   events Event[]
// }

// unfinished, havent put relation many to many between user and participants
model participants {
  id String @id @default(uuid())
  userId String
  user User @relation(fields: [userId], references: [id])
  joinedAt DateTime @default(now())
}

//after update this schema dont forget to push:
// npx prisma db push